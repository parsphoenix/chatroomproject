# 🚀 راهنمای نصب وب‌چت

## مراحل نصب

### 1. آپلود فایل‌ها
- تمام فایل‌های پروژه را در روت هاست خود آپلود کنید
- مطمئن شوید که پوشه `uploads` قابل نوشتن است (chmod 755 یا 777)

### 2. تنظیمات دیتابیس
- یک دیتابیس MySQL جدید ایجاد کنید
- نام کاربری و رمز عبور دیتابیس را آماده کنید

### 3. شروع نصب
- به آدرس سایت خود بروید: `http://yourdomain.com`
- اگر نصب نشده باشد، خودکار به صفحه نصب منتقل می‌شوید
- یا مستقیماً به `http://yourdomain.com/install.php` بروید

### 4. تکمیل فرم نصب
اطلاعات زیر را وارد کنید:

#### اطلاعات دیتابیس:
- **آدرس سرور**: معمولاً `localhost`
- **نام دیتابیس**: نام دیتابیس ایجاد شده
- **نام کاربری**: نام کاربری دیتابیس
- **رمز عبور**: رمز عبور دیتابیس
- **پورت**: معمولاً `3306`

#### حساب مدیر اولیه:
- **نام کاربری ادمین**: نام کاربری مدیر (3-50 کاراکتر)
- **رمز عبور ادمین**: رمز عبور مدیر (حداقل 6 کاراکتر)

### 5. تکمیل نصب
- روی دکمه "نصب و راه‌اندازی" کلیک کنید
- منتظر بمانید تا نصب کامل شود
- خودکار به صفحه ورود منتقل می‌شوید

## مشکلات رایج و راه‌حل

### مشکل: صفحه نصب نمایش داده نمی‌شود
**راه‌حل:**
1. فایل `installed.lock` را حذف کنید
2. فایل `config/db.php` را حذف کنید
3. مجدداً سعی کنید

### مشکل: خطای اتصال به دیتابیس
**راه‌حل:**
1. اطلاعات دیتابیس را بررسی کنید
2. مطمئن شوید دیتابیس ایجاد شده است
3. دسترسی‌های کاربر دیتابیس را بررسی کنید

### مشکل: خطای مجوز فایل
**راه‌حل:**
1. مجوز پوشه `uploads` را به 755 تغییر دهید
2. مجوز پوشه `config` را به 755 تغییر دهید

## فایل‌های تست

### تست وضعیت نصب:
```
http://yourdomain.com/test_install.php
```

### تست عملکرد سیستم:
```
http://yourdomain.com/test.php
```

## پس از نصب

### ورود به سیستم:
- با نام کاربری و رمز عبور ادمین وارد شوید
- از داشبورد به پنل ادمین دسترسی خواهید داشت

### ایجاد کاربران:
- کاربران می‌توانند از صفحه ثبت‌نام حساب ایجاد کنند
- یا ادمین می‌تواند کاربران را مدیریت کند

### تنظیمات امنیتی:
- فایل `installed.lock` را حذف نکنید
- فایل `config/db.php` را محافظت کنید
- رمزهای قوی استفاده کنید

## پشتیبانی

اگر مشکلی داشتید:
1. فایل‌های لاگ سرور را بررسی کنید
2. فایل `debug.php` را اجرا کنید
3. از فایل `test_install.php` برای تشخیص مشکل استفاده کنید

---

**نکته مهم:** پس از نصب موفق، فایل‌های `install.php` و `test_install.php` را می‌توانید حذف کنید تا امنیت بیشتری داشته باشید.